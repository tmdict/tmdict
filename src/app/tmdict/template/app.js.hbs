import App from '{{level}}../../js/components/App.svelte';
import data from '{{level}}../data/{{path}}/app{{appHash}}.js';
let app = new App({
  target: document.body,
  props: {
    data: data
  }
});
