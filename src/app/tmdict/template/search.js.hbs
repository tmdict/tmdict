import Search from '{{level}}../../../js/components/Search.svelte';
import data from '{{level}}../../data/{{path}}/search{{searchHash}}.js';
let app = new Search({
  target: document.querySelector('#search'),
  props: {
    data: data
  }
});
